<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item android:id="@+id/Mensajes"
            android:icon="@drawable/baseline_message"
            android:title="Mensajes">
        </item>
        <item android:id="@+id/Explorer"
            android:icon="@drawable/baseline_explore"
            android:title="Explorar">
        </item>
        <item android:id="@+id/Comentarios"
            android:icon="@drawable/baseline_add_comment"
            android:title="Comentarios">
        </item>
    </group>

    <item android:title="Redes Sociales">
        <menu>
            <item android:id="@+id/Enviar"
                android:icon="@drawable/baseline_send"
                android:title="Enviar">
            </item>
            <item android:id="@+id/Compartir"
                android:icon="@drawable/baseline_share"
                android:title="Compartir">
            </item>
        </menu>
    </item>
</menu>